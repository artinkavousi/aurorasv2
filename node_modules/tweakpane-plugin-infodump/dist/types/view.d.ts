import { View, ViewProps } from '@tweakpane/core';
interface Config {
    content: string;
    markdown: boolean;
    viewProps: ViewProps;
}
export declare class InfodumpView implements View {
    readonly element: HTMLElement;
    constructor(doc: Document, config: Config);
}
export {};
